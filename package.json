{
  "name": "rix-nb",
  "version": "0.1.0",
  "description": "Native macOS Markdown and RiX notebook authoring environment",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "sync:help": "bun scripts/sync-help.mjs",
    "build:live-runtime": "vite build --config vite.live.config.js",
    "dev:web": "bun run sync:help && bun run build:live-runtime && vite --host 127.0.0.1 --port 1420",
    "build": "tauri build",
    "build:web": "bun run sync:help && bun run build:live-runtime && vite build",
    "dev:browser": "vite --config vite.browser.config.js --host 127.0.0.1 --port 1421",
    "build:browser": "vite build --config vite.browser.config.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@codemirror/autocomplete": "^6.20.3",
    "@tauri-apps/api": "^2.8.0",
    "@codemirror/lang-markdown": "^6.3.3",
    "@codemirror/lint": "^6.9.7",
    "@codemirror/state": "^6.5.2",
    "@codemirror/view": "^6.36.5",
    "@tauri-apps/plugin-dialog": "^2.4.0",
    "@tauri-apps/plugin-fs": "^2.4.0",
    "codemirror": "^6.0.1",
    "katex": "^0.17.0",
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0",
    "vite": "^6.1.0"
  },
  "engines": {
    "bun": ">=1.2.0"
  },
  "license": "MIT"
}
